my.h为常用头文件。
1-4.c测试了构造函数析构函数回滚函数，以及验证exit(0);_exit(0);return 0;三种方式退出时缓存区情况。
1-5.c测试fork派生子进程时，父子进程共享数据情况，在子进程修改父进程普通变量、静态变量、全局变量，然后分别在父子进程输出对比。
1-6.c测试_fork派生子进程时，父子进程共享数据情况，在子进程修改父进程普通变量、静态变量、全局变量，然后分别在父子进程输出对比。
ss.c验证_vfork死锁缺陷，父进程等待子进程结束，子进程只有先执行父进程的代码才可结束。
