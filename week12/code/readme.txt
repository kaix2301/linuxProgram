my.h为常用头文件。

11-1验证管道缓存区大小，不断向管道写数据，写入第65536个数据后阻塞，故缓存区大小为64kb。

2pipe3son.c中子进程1向管道1写入0-4095；子进程2从管道1读出数据并输出，然后保存到管道2；子进程3从管道2读取数据求和后输出结果。

1pipe3son.c进程功能和2pipe3son.c一致，只用一个管道实现。

12-4.c给信号绑定处理函数;让子进程sleep，然后在/proc/pid下查看管道文件。

12-5.c验证管道操作非原子性，三个子进程同时向同一管道写数据，父进程从管道读取数据写入文件，最后文件中三个进程写入管道的数据交替出现。
